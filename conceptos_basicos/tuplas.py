# Una tupla es una estructura de datos y un tipo de dato que permite almacenar cualquier tipo de valor ya
# sea enteros, flotantes, cadenas, listas e incluso funciones. Se caracteriza por ser una lista inmutable.
#
# Estructura =>       variable = (datos, datos, datos ...)

tupla = ("camilo", 2, 3.345, 'a', "jaime")
lista = list(tupla)
#tupla = tuple(lista)

print (tupla[:])
print (lista[:])
